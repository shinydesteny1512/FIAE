package cgi.game.creations.items.accessiore;

import cgi.game.instance.level.GameInstance;

public class Belt extends Accessoires {
    public Belt(GameInstance instance) {
        super(instance);
    }
}
